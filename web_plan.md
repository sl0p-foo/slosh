ok listen up dude,

we're going to need a good webpage for this slosh thing

we have some semi good (generated) docs/ website thing already

also we have very cool web-based linux demo system in ./contrib/webdemo/

we should integrate all of these to a good single page website (minus the live demo thing maybe can live on its own page to reduce innitial clutter0

page will be simple in terms of getting the point accross what slosh is/does

we'll render a good demo video (TBD, put placeholder) 

some `curl http://slosh.foo/install.sh | sudo sh` instructions

make sure it doesn't look like 1 of those thousand a dozen claude-opus webpages

super tired of those

lets go, u put artifacts in ./web_new (mkdir, not exist yet)
